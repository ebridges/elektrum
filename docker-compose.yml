version: '3'

services:
  elektron_app:
    image: roja/elektron:latest
    networks:
      - proxy_network

  elektron_proxy:
    image: nginx:1.13
    ports:
      - 80:80
    volumes:
      - ./etc/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - elektron_app
    networks:
      - proxy_network

networks:
  proxy_network:
    driver: bridge
